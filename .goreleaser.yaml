project_name: sumcheck

builds:
  - env: [CGO_ENABLED=0]
    goos: [linux, windows, darwin, macos]
    goarch: [amd64, arm64]

archives:
  - format_overrides:
      - goos: windows
        format: zip

release:
  github:
    owner: henristr
    name: sumcheck
